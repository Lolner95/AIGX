// AIGX example fixture: filterParser.ts
// This file's boundary (role / forbid / gotcha / checks) is declared in .aigx/files.aigx.
// It exists so the genome validates against a real tree (aigx lint).
